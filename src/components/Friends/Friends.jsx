import React from 'react';
import { Bar } from '../';
import './Friends.scss';

export default props => {
    return (
        <Bar>
            <div className="friends"></div>
        </Bar>
    );
};
